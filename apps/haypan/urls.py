from django.urls import path
from . import views

urlpatterns = [
    # path('login/user1/', views.User1LoginView.as_view(), name='user1_login'),
    # path('login/user2/', views.User2LoginView.as_view(), name='user2_login'),
    # path('register/user1/', views.User1RegisterView.as_view(), name='user1_register'),
    # path('register/user2/', views.User2RegisterView.as_view(), name='user2_register'),
]
